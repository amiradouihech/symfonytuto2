{% extends "base.html.twig" %}
{% block body %}
<h1>Liste des users </h1>
<table class="table">
  <thead>
    <tr>
      <th scope="col">titre</th>
      <th scope="col">resume</th>
      <th scope="col">date_publication</th>
      <th scope="col">auteur</th>
    </tr>
  </thead>
  <tbody>
  {% for a in ar %}
    <tr>
    <td>{{a.titre|title}}{% if date(a.date_publication)<date("-2 years") %}<em> Archivés<em>{% endif %}</td>
      <td>{{a.resume}}</td>
      <td>
      {{date(a.date_publication)|date('d/m/Y')}}
      </td>
      <td>{{a.auteur}}</td>
    </tr>
      
    {% endfor %}
  </tbody>
</table>
{% endblock %}